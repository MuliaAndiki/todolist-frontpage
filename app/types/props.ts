import { BenefitsCardType, ExperiensCardType, TodolistType } from './components';

export interface BenefitsCardProps {
  data: BenefitsCardType;
}

export interface ExperiensCardProps {
  data: ExperiensCardType;
}

export interface TodolistProps {
  data: TodolistType;
}
